# ElevenLabs Configuration
# Copy this file to .env and fill in your actual values
# Make sure NOT to save this file to your repository and is in all the proper "ignore" files such as .gitignore

# ElevenLabs Embed Code
# <elevenlabs-convai agent-id="agent_xyz01334"></elevenlabs-convai><script src="https://unpkg.com/@elevenlabs/convai-widget-embed@beta" async type="text/javascript"></script>

# ============================================================
# CLIENT-SIDE VARIABLES (VITE_ prefix - exposed to browser)
# ============================================================

# ElevenLabs Agent ID for voice conversations
# Get this from your ElevenLabs dashboard under Conversational AI
VITE_ELEVENLABS_AGENT_ID=agent_your_agent_id_here

# API base URL for the backend server (defaults to http://localhost:3001)
VITE_API_BASE_URL=http://localhost:3001

# Development mode flag
VITE_NODE_ENV=development

# Voice Agent Connection Mode
# Controls how the app communicates with ElevenLabs
# Options:
#   "agent-sdk" - Current default: Direct connection via @elevenlabs/react SDK using Agent ID
#   "widget" - ElevenLabs embed widget (web component) with customizable orb UI
VITE_VOICE_CONNECTION_MODE=agent-sdk

# ============================================================
# WIDGET MODE CONFIGURATION (only used when VITE_VOICE_CONNECTION_MODE=widget)
# ============================================================

# Widget Visual Customization - Acoustic Noir Theme
# Orb gradient colors (hex format, matches your design system)
VITE_WIDGET_ORB_COLOR_1=#fbbf24
VITE_WIDGET_ORB_COLOR_2=#a78bfa

# Widget variant: "compact" (floating orb) or "expanded" (full panel)
VITE_WIDGET_VARIANT=compact

# Widget Text Labels (customize to match your brand voice)
VITE_WIDGET_ACTION_TEXT=Talk to AI
VITE_WIDGET_START_CALL_TEXT=Start Conversation
VITE_WIDGET_END_CALL_TEXT=End Conversation
VITE_WIDGET_LISTENING_TEXT=Listening...
VITE_WIDGET_SPEAKING_TEXT=Speaking...

# Optional: Custom avatar image URL (leave empty to use orb)
VITE_WIDGET_AVATAR_URL=

# Optional: Override voice settings at runtime
VITE_WIDGET_OVERRIDE_VOICE_ID=
VITE_WIDGET_OVERRIDE_LANGUAGE=
# Override first message - Required if your agent uses dynamic variables like {agent_name}, {user_name}, {greeting}
# Set this to bypass the dynamic variable requirement in widget mode
VITE_WIDGET_OVERRIDE_FIRST_MESSAGE=Hello! How can I help you today?

# ============================================================
# SERVER-SIDE VARIABLES (NOT exposed to client - secure)
# ============================================================

# ElevenLabs API Key - REQUIRED for signed URL authentication
# Get this from https://elevenlabs.io/app/settings/api-keys
# This key is kept server-side only and never exposed to the browser
ELEVENLABS_API_KEY=sk_your_api_key_here

# Server configuration
SERVER_PORT=3001
CORS_ORIGIN=http://localhost:8082